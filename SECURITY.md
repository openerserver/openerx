# 安全说明

## 报告问题

请通过仓库的 [私密漏洞报告入口](https://github.com/openerserver/openerx/security/advisories/new) 提交可复现的安全问题。如果该入口未开放，请先在 Issue 中请求维护者提供私密联系方式，不要公开利用细节、令牌或私人文件。

报告应包含版本、操作系统、复现步骤、预期权限边界和实际结果。不要附完整用户配置目录；日志、截图和样例必须脱敏。

## 数据与凭据

- 默认模式直接连接用户配置的模型服务。提示词、相关附件和工具结果可能发送给该服务。
- API Key 和 MCP 敏感配置使用操作系统保护的本地凭证存储；不要将它们写入源码、普通配置或 Issue。
- 对话、项目与文件元数据由本地存储管理。远程 MCP、模型以及显式配置的平台服务各有自己的数据处理范围。
- 记忆和模型输出可能出错。重要操作应检查结果，并保持关键文件备份。

## 执行边界

Renderer 通过类型化 Preload Bridge 访问能力，不直接拥有 Node、文件系统或原始 IPC 权限。Main 管理系统集成、凭据和进程；本地 App Service 管理业务数据；Pi Host 通过权限 Broker 请求工具执行。

网页、附件、模型输出、MCP 返回值及外部 Skill 都是不可信输入。安装工具或 Skill 不会自动扩大文件、Shell、浏览器或桌面权限。模型也不能给自己批准操作。

平台权限、操作系统完整性和用户授予的范围共同决定实际保护能力。已被管理员权限控制的操作系统不在应用能够独立防御的范围内。

## 发布

维护者需要核对依赖、原生签名、公证、更新清单和目标平台测试结果。开发运行成功不等同于已完成签名发布；发布流程见 [构建与发布](docs/releasing.md)。
