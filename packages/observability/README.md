# Observability

脱敏日志、诊断事件和可回溯运行指标的共享边界。

- 诊断只保存白名单生命周期事件，键和值双重脱敏，最多导出最近 1,000 条。
- 用户必须先看到包含/排除项和性能状态，再经 Electron Main 保存诊断包。
- 个人数据 ZIP 与诊断包分离；结构化记录和本机受控对象字节可导出，对话正文永不进入诊断包。
- Token、报价、费用、余额和账单由服务端计算并保存，不进入本地个人数据导出。
