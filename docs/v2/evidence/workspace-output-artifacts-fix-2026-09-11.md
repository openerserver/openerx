# 工作区输出文件归集修复（2026-09-11，Asia/Shanghai）

原对话通过工作区补丁生成了网站文件，但成果列表只识别专用成果工具的调用记录，导致输出区为空，手工导入的文件则显示在来源区。

修复增加本地 `workspace_artifact_links` 关联：从该对话已经应用的补丁、命令行文件变更及已应用的隔离变更集中识别文件，检查现有目录授权、路径边界和内容摘要，然后保存为普通成果。首次打开历史对话时自动回补；后续写入复用成果 ID，内容变化时增加不可变版本。来源附件、失败或未应用的变更不作为回补依据。已保存的成果快照保留，不因源目录撤权而丢失。

前台和后台 Shell 增加文件变更记录；依赖目录不参与这条新增扫描路径。界面移除只显示前六个成果的限制。保存单个文件时使用文件名作为另存默认值，图片成果支持图像预览。

## 输出目录

对话侧栏将全部成果收在一个默认的“输出文件”目录中，根目录文件也在其中。使用成果已保存的相对路径构建可折叠的子目录，文件夹优先、名称排序，显示目录内全部文件数量；文件行显示文件名，悬停保留完整相对路径。默认展开输出目录、折叠子目录，支持键盘操作；预览后返回保留展开状态，切换对话重新初始化。此整理只改变浏览视图，成果 ID、版本、磁盘位置和 HTML 资源路径不变。

目录回归使用原网站 12 个成果及独立的 9 个资源用例：21 个文件均位于默认目录，验证多级目录、同名 HTML 在不同目录中的独立预览、键盘折叠/展开、预览返回状态，以及多级 CSS import。更新后的 Electron 记录见 [HTML 与目录回归记录](./html-output-preview-e2e-2026-09-11.json)，截图保存于 `/Users/wanglei/Downloads/phones-cloud/openerx-checkpoints/output-directories-20260911-160654/verification-evidence/`。

## 验证

- 5 个新增场景通过：历史网站文件回补及预览导出；版本、路径和对话隔离；撤权/符号链接/文件缺失/内容变化/撤销；隔离变更集及附加目录；真实 macOS 前台和后台 Shell 文件生成。
- 91 项相关存储、文件、对话、工具与工作区测试通过；76 项桌面聊天界面测试通过，共 172 项不同测试。
- app-service、storage、tool-sdk、file-service、desktop 类型检查通过；仓库边界检查通过。原有 App.tsx 有一项 lint warning 和一项 info，本次新增代码格式检查通过。
- 构建并校验本地 macOS arm64 应用；真实数据副本的桌面测试显示 12 个输出，源码预览成功，重复查询无新增版本，无页面脚本错误。
- 已更新本机应用并打开原对话。实际界面显示 12 个输出；逐个比对成果与工作区文件 SHA-256，全部一致；SQLite quick_check 为 ok，schema 为 34。

结构化记录：[workspace-output-artifacts-e2e-2026-09-11.json](./workspace-output-artifacts-e2e-2026-09-11.json)。可复用桌面验证入口：`apps/desktop/scripts/e2e-workspace-outputs.mjs`，参数为指定可丢弃数据副本的 JSON 配置路径。

## 本地边界与恢复

本次证据为本地桌面验证，不代表发布或跨设备验证。来源关联包含本地工作区路径，不同步目录授权。历史回补需要仍有效的授权及与已记录写入摘要一致的文件；没有写入证据的历史 Shell 文件不会通过扫描目录猜测归属。Shell 追踪沿用现有变更追踪器的容量上限，未取得真实文件摘要的超大文件不自动收录。

更新时一次本机服务启动超时；重启后日志记录 2026-09-11T06:56:26.250Z service.ready，原对话恢复。启动期间模型配置曾等待系统凭据读取；最终界面已正常显示 DeepSeek V4 Flash，模型菜单恢复可用。未自动批准凭据访问，也未更改用户密钥。

旧应用、更新前 SQLite 数据库和内容副本、初始未提交改动归档保存在 `/Users/wanglei/Downloads/phones-cloud/openerx-checkpoints/workspace-outputs-20260911-143505/`。数据库已升级，回退旧应用时须同时使用 `profile-before-activation` 数据副本。原有未提交改动已保留，文件成果修复按独立范围提交。
