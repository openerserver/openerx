# M2 Account, Sync, Model and Usage evidence

> Status: `PASS (local implementation checkpoint)`
>
> Checkpoint date: 2026-08-25 (Asia/Shanghai)
>
> Scope: account/device session, cross-device content sync, Pi-native Platform Model Provider,
> authoritative Token usage, desktop account UI, and versioned Remote foundation contracts

## Gate result

| M2 requirement | Result | Automated evidence |
| --- | --- | --- |
| Email identity and rotating device session | PASS | `services/identity-api/tests/identity-service.test.ts`; `apps/desktop/tests/account-session-manager.test.ts` |
| Device inventory, targeted revoke and all-device sign-out | PASS | Identity and Account Session Manager tests; Electron account E2E verifies revoked-token rejection |
| OS-protected reusable credential | PASS | `apps/desktop/tests/credential-vault.test.ts`; Electron account E2E verifies encrypted bytes and deletion on sign-out |
| Local-first account sync, conflict resolution and deletion boundary | PASS | `tests/v2/golden/account-m2.test.ts`; `services/account-sync-api/tests/account-sync-service.test.ts` |
| Concurrent-sync race safety | PASS | `packages/app-service/tests/sync-coordinator.test.ts`; Pull preserves local pending/conflict versions |
| Automatic/explicit model selection, effective model and capability gate | PASS | `services/model-gateway/tests/model-gateway-service.test.ts`; Pi Provider integration test; Electron account E2E |
| Authoritative, deduplicated Token usage and account aggregate | PASS | `services/token-usage-store/tests/usage-store.test.ts`; Platform HTTP, account UI and Electron E2E |
| Secret/error redaction | PASS | `packages/contracts/tests/contracts.test.ts` exercises Bearer, refresh credential, API key, `sk-` and URL-token canaries |
| Account-scoped desktop profile | PASS | login switches supervised App Service profile; restart refreshes the device session and restores the same conversation |
| Remote protocol foundation | PASS | strict V1 schemas in `packages/contracts/src/remote.ts`; expiry, version, replay-shape and encrypted-envelope tests |
| Production process and package boundary | PASS | sibling-service boundary check, hardened Electron Fuses, production ASAR test-provider exclusion |

## GT-ACCOUNT-01 through GT-ACCOUNT-10

| Task | M2 local implementation proof |
| --- | --- |
| [GT-ACCOUNT-01](golden/local-implementation/GT-ACCOUNT-01.md) | login, refresh rotation/replay revocation, protected credential and restart refresh |
| [GT-ACCOUNT-02](golden/local-implementation/GT-ACCOUNT-02.md) | independent replicas restore the M2 Conversation/Branch/Message/model slice |
| [GT-ACCOUNT-03](golden/local-implementation/GT-ACCOUNT-03.md) | transactional Outbox, offline queue and idempotent operation replay |
| [GT-ACCOUNT-04](golden/local-implementation/GT-ACCOUNT-04.md) | visible conflict plus explicit local/cloud resolution |
| [GT-ACCOUNT-05](golden/local-implementation/GT-ACCOUNT-05.md) | device revoke/all sign-out and account isolation |
| [GT-ACCOUNT-06](golden/local-implementation/GT-ACCOUNT-06.md) | automatic/explicit selection and selected/effective model evidence |
| [GT-ACCOUNT-07](golden/local-implementation/GT-ACCOUNT-07.md) | pre-execution capability block and explicit-only fallback |
| [GT-ACCOUNT-08](golden/local-implementation/GT-ACCOUNT-08.md) | message/conversation Token classes preserve unknown values |
| [GT-ACCOUNT-09](golden/local-implementation/GT-ACCOUNT-09.md) | dedupe plus message/conversation/account aggregates |
| [GT-ACCOUNT-10](golden/local-implementation/GT-ACCOUNT-10.md) | cache clear, device exit and retained-tombstone cloud deletion boundaries |

Attachment, Artifact, file replay and presigned-resource revocation remain M4 scope. Real file/tool
capability switching remains M4/M5 scope. The M2 evidence therefore proves only the objects and
pre-execution model gate implemented at this checkpoint, without claiming those later slices early.

## Implemented request path

```text
React Renderer
  -> typed Preload account/chat/model/usage methods
  -> Electron Main + OS safeStorage credential boundary
  -> account-scoped App Service SQLite + transactional Outbox
  -> Account Sync API (revision/cursor/conflict/tombstone)
  -> isolated Pi Host
  -> Pi AgentSession + native Platform Provider
  -> Platform Model Gateway
  -> provider-reported UsageRecord -> Token Usage Store
```

No upstream Provider API key enters the desktop client. Pi still owns the agent loop; the platform
integration is a Pi-native Provider registered in `ModelRuntime`, not a parallel model adapter or
execution loop.

## Reproducible checks

Run from the repository root:

```bash
npm run check:boundaries:v2
npm run lint:v2
npm run typecheck:v2
npm run test:v2
npm run build:v2
npm run test:e2e:v2
npm run verify:package:v2
npm run audit:prod:v2
node_modules/.bin/asar list apps/desktop/out/OpenerX-darwin-arm64/OpenerX.app/Contents/Resources/app.asar | rg 'pi-host|platform-alpha-test|pi-host-test|providers/faux'
git diff --check
```

Observed at this checkpoint:

- boundary check passed across 81 source files with no Legacy or sibling app/service import;
- Biome passed across 118 files and all 11 implemented workspaces passed strict TypeScript checks;
- Vitest passed 72 tests across workspace, service, integration and Golden suites;
- Electron E2E passed `E2E_CHAT_OK` and
  `E2E_ACCOUNT_OK login-keychain-auto-model-effective-usage-device-revoke-cache-cloud-delete-signout-all`;
- production dependency audit reported 0 vulnerabilities;
- macOS arm64 production package rebuilt; current macOS arm64/x64 and Windows x64 artifacts passed
  Electron Fuse verification;
- production ASAR contains the production Pi Host and excludes the test Pi Host, Pi faux Provider and
  Platform Alpha fixture;
- `git diff --check` passed.

## Evidence boundary

The Platform Alpha E2E uses a deterministic test-only upstream executor behind the real HTTP Model
Gateway, Usage Store and Pi Provider path. Its second device is an independent service session and
replica, not a native Windows host. External paid-provider validation, hosted Windows/macOS CI,
native two-device execution, package signing and notarization are release-environment gates and are
not claimed by this local checkpoint.

Remote M2 scope freezes strict device, pairing, signed command, receipt, attention, encrypted product
event, cursor and push-subscription contracts. Mobile apps, Host Connector, Relay, push delivery and
real device pairing remain M6.

## Next checkpoint

M3 Billing Alpha adds versioned pricing, quotes, reservations, Usage-to-Charge settlement, quota,
points, cash balance, append-only double-entry ledger, payment adapter and statements. M4 File and
Artifact work may proceed in parallel after this M2 boundary.
