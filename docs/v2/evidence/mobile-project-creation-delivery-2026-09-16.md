# 移动端新建项目交付 · 2026-09-16

用户要求“提交，更新”后，已更新本机两版桌面、连接的 iPhone 上的 UWA，以及现有生产中央 Data API。本文补充此前的[实现与回归记录](mobile-project-creation-2026-09-16.md)。时间使用 Asia/Shanghai。

## 源码与构建

| 组件 | 已交付源码 | 版本 / 构建 |
| --- | --- | --- |
| openerx 桌面 | `cbc80cc50438ca1b9302a9e961cfce07c62d437e` | 2.0.5 / `20260916T065632Z` |
| UWA 桌面、移动端、Data API | `7d8f2922a4d2a1e62a81100c17d09999f6aaef6e` | 2.1.2 / `20260916T065632Z` |
| UWA core | `c40fca634fbad86f469e625c64dab04310875749` | 外层仓库已记录该指针 |

两版桌面均通过仓库根目录 `npm run package:v2` 更新既有固定输出，打包前停止使用该输出的应用。UWA 同时覆盖更新 `~/Applications/UWA Enterprise.app`。两版签名完整性和生产 Electron fuses 检查通过；真实桌面设置页读回上表版本、构建时间和代码提交。应用已正常启动。此为本机签名包，未做商店或公证发布。

openerx 保持本机模式；没有为它新设账号服务或网关。UWA 保留中央账号、`OpenerX-Enterprise` 数据目录及现有手机连接。桌面一致性检查对当前 openerx 源码通过：444 个共享文件、115 个已审阅差异。

## 移动端与线上服务

- UWA iOS Release 实体设备包、模拟器包均构建成功。JS 入口均为 `apps/mobile/index.ts`，内嵌构建信息和 bundle SHA-256 校验一致。实体包签名验证通过，保留数据覆盖安装到已连接 iPhone Air，设备读回 2.1.2 / `2450.6.56`。
- 实体 iPhone 锁屏，系统拒绝远程启动；已安装不等于真机操作验收。本轮端到端操作证据来自 iPhone 17 Pro / iOS 26.2 模拟器。
- Android arm64 构建通过，选定提交的 APK SHA-256 为 `16536f1cd55c607e90e0a59b92866f232178509a6d86622e2e36110e8b287a73`。模拟器已有另一任务安装的较新版本码 `3528426`，本次版本码为 `3528417`；没有降级或清除其数据，恢复保留了较新安装包。未连接实体 Android 设备。
- 生产入口为 `https://api2.phones-cloud.cn`，运行主机为 `shell2.phones-cloud.cn`。只替换并重启现有 Data API，保留 Auth、worker、短信、Nginx、配置和数据库。新发布为 `/opt/openerx-enterprise/releases/20260916T070538Z-mobile-project-creation`，上一发布保留以供回滚。
- 公网 `/api/v3/version` 返回 2.1.2、`20260916T065632Z`、`7d8f2922…`、`dirty:false`；运行中 Data 可执行文件 SHA-256 与构建一致：`ed09eb519a5f661b41d5a16ca6d817dc22d44ec4d423e8195bcfca7739dfffd5`。健康检查、未认证请求的 401、原密码策略及全部既有服务状态检查通过。Auth 仍为先前的 `20260916T030444Z`。

构建期间另一任务在同一目录修改移动端品牌界面。后续手机包和服务器使用固定提交的源码快照构建，复用已有依赖和固定原生输出，不包含该任务未提交的界面修改。初次服务构建受原生版本戳写入影响而带有不同的 Go VCS dirty 标记；上线校验自动回滚，之后从固定源码重新构建并通过校验。未覆盖并行任务的源码，也未将其纳入本次提交。

## 实际创建验证与数据

已用原生 iOS 模拟器、现有已配对账号、线上 HTTPS 服务和更新后的真实 UWA 桌面验证“项目 → 新建项目 → 创建”。没有替换认证、服务或桌面执行方，没有调用模型。

最终验证项目 `Test` 逐字输入并结束编辑后提交，桌面持久化名称与输入完全一致；远程命令 `35cc3130-0a83-4900-b352-4d5310232b44` 记录为 `applied`，桌面项目概览显示 0 个对话、0 个目录。手机成功回执与桌面落库均已观察。

首次快速自动输入的 AX 值曾显示 `Qa0916`，但实际提交并保存为 `Q`；不能将那次操作记作完整名称输入通过。后续逐字输入 `Test` 的复验通过。该自动输入现象未在实体手机复验。过程消息中的测试名称以本记录的数据库核对结果为准。

两个空白测试项目 `Q`、`Test` 均已通过桌面界面归档，未永久删除。两版数据库完整性检查均为 `ok`，原有聊天和消息表摘要未变化，原有项目保留。新增记录仅为两次验证创建及归档的相关数据；登录 session 的加密内容可随正常刷新变化。

本地详细证据位于 `.codex-temp/mobile-project-creation-update/`，包括构建、安装、部署、公开版本、真实创建命令和前后数据摘要。无 OTA 或商店发布。
