# PBASH-007 Golden A/B 与渐进启用证据

- 日期：2026-08-28（Asia/Shanghai）
- Git 分支：`codex/pbash-001-20260828`
- 状态：`LOCAL DETERMINISTIC COMPLETE / MODEL A/B EXTERNAL PENDING / NOT RELEASE EVIDENCE`
- 前置检查点：`c8269bc`（PBASH-006）

## 1. 固定 A/B 边界

本检查点使用同一临时工作区、同一命令文本、同一 15 秒 timeout 和默认断网，对比：

- A：现有 `ShellToolAdapter`，通过 argv 启动 `/bin/bash --noprofile --norc -c`；
- B：`MacOSSandboxExecEngine`，使用 `read_only + core + network deny` 执行 Brokered `bash`。

固定任务覆盖仓库探索、组合搜索、构建、测试、lint、失败诊断、补丁前验证、长输出和停止。每个
Brokered 结果都必须证明 filesystem boundary、environment sanitized、network denied，且 controlled
egress 未开启。测试没有调用模型或付费 Provider，所以模型轮次、Token、模型工具选择质量和 Provider
成本均为 `N/A`；这些指标仍是外部 Golden 门禁，不能由本测试代替。

## 2. 实际结果

| 任务 | 旧 `openerx_shell` 路径 | Brokered `bash` | 结论 |
| --- | --- | --- | --- |
| 仓库探索 | PASS | PASS | 对齐 |
| 组合搜索 | PASS | PASS | 对齐 |
| 构建语法检查 | PASS | PASS | 对齐 |
| 测试 | PASS | PASS | 对齐 |
| lint 语法检查 | PASS | PASS | 对齐 |
| 失败诊断与 exit 7 | PASS | PASS | 对齐 |
| 补丁前 `git diff --check` | FAIL，exit 128 | PASS | 已解释改善 |
| 250 行长输出 | PASS | PASS | 对齐 |
| 停止活动任务 | PASS | PASS | 对齐，Broker 销毁状态非 uncertain |

汇总：旧路径 8/9，Brokered 路径 9/9；安全越界 0，未解释回归 0。旧路径唯一失败可重复为
`fatal: could not open '/dev/null' for reading and writing: Operation not permitted`，来自旧 Seatbelt profile；
新 Runner 的最小设备边界允许 Git 完成验证，但仍保持工作区外文件、网络和 Secret 拒绝。

定向 Golden 测试与 Shell 进程回收测试共同通过：3 个测试文件、26 个测试。Golden 文件单独运行时
2 个测试通过；总耗时只作为当前主机测试时长，不外推为模型任务性能。

## 3. 渐进启用与回滚

- `OPENERX_BROKERED_BASH_V1` 继续默认关闭；`undefined/0/false` 为关闭，`1/true` 为开启。
- flag 关闭时只投影 `openerx_shell`/`openerx_shell_process`；开启且 Runner probe 通过时只投影 `bash`。
- 开启但 Runner probe 失败时两类 Shell 都不投影，不静默回退到旧 Shell 或 Pi 内置 Bash。
- 关闭 flag 不迁移、不删除、不重放历史 ToolCall；回滚只改变下一次 Generation 的工具投影。
- 工具中心现在展示 Local Alpha、Runner、sandbox policy、core Secret-filtered 环境、默认断网和有效
  workspace 数；关闭时展示 `openerx_shell` 回滚路径和“不会同时暴露”提示。

App Service 与 Desktop 定向结果：`tool-app-service.test.ts` 16 个测试通过，`chat-ui.test.tsx` 30 个
测试通过；Contracts、Tool SDK、App Service、Desktop typecheck 通过。

## 4. 指标边界

| 指标 | 本机确定性结果 | 是否可作为模型结论 |
| --- | --- | --- |
| 固定任务完成率 | legacy 8/9；brokered 9/9 | 只能说明 Runner 兼容性 |
| 安全越界 | 0 | 是，本机合同范围内 |
| 未解释回归 | 0 | 是，本机固定命令范围内 |
| 模型轮次/工具调用/无效调用 | N/A | 否，需固定模型实测 |
| Token/Provider 成本 | N/A | 否，需真实 Provider |
| 人工审批次数 | 0 | 只适用于本地只读固定集 |
| Remote/签名包/跨 OS | 未运行 | 否，仍是外部门禁 |

## 5. 未外推结论

- PBASH-007 的本地确定性部分完成，不代表真实模型在长上下文、工具选择或修复质量上已经完成 A/B。
- 本结果来自当前未签名 macOS 开发环境；不代表 Developer ID、公证、Linux、WSL2 或 Windows 支持。
- `/usr/bin/sandbox-exec` 仍是 deprecated 实现细节。签名包、OS 矩阵和替代后端路线属于 PBASH-008。
