# 开源发布准备验证记录

验证日期：2026-09-05。范围：OpenERX 2.0.1 开源核心，Windows x64，Node 24.19.0 / npm 11.3.0。以下是本地验证，不是已在 GitHub 发布或跨平台验收完成的声明。

| 检查 | 结果 |
| --- | --- |
| `npm ci` 干净安装 | 通过；锁文件包含跨平台可选依赖与 integrity |
| `npm run check:source` | 通过；本地报告 721 个测试通过、2 个条件跳过 |
| 全依赖 `npm audit --audit-level=high` | 0 个已报告漏洞（包含开发/构建依赖，结果具有时效性） |
| 源码公开路径/可达历史检查 | 通过；未包含备份、移动端、企业目录或匹配规则的凭据 |
| 第三方清单一致性 | 851 个外部依赖路径；不等于人工许可审核完成 |
| TypeScript / Biome | 类型检查通过、lint 无错误；保留既有 CSS 选择器顺序警告 |
| 本地 release graph/readiness | 原检查通过；公开桌面范围调整为 9 组正式发行外部证据，均仍待完成 |
| Windows 真实打包 | 通过；使用独立输出目录，未覆盖被占用的旧包 |
| ASAR / Electron fuses / 随包许可 | 通过；本地包未签名、自动更新关闭 |
| 真实包 UI 与服务冒烟 | 自动化导航/列表、5 个 Skill 卡片、记忆设置开关、保存和重启后读取均通过 |
| 数据隔离 | 冒烟只使用新建临时 profile；未修改日常用户数据库 |
| 未签名归档 | `.tar.gz` 与 `SHA256SUMS` 生成通过 |
| Workflow/Issue/Dependabot YAML | 解析通过；尚未在 GitHub runner 执行 |

## 仍需外部确认

- 首次 GitHub push/远端地址、仓库保护、安全私报及 CI 实跑。
- macOS arm64/x64 实机/runner 验证、签名、公证、安装/升级/回滚。
- 原创代码及素材发布权；[第三方许可例外审核](../third-party/README.md) 当前未批准，`npm run notices:release` 按预期阻止正式签名候选流程。
- 正式发布 tag、发行说明日期、更新托管与人工验收。详细步骤见 [发布清单](PUBLISHING.md)。

此记录只反映当次验证。依赖、运行时、权限、系统或代码变更后需重新执行检查；不能复用这份记录替代新版本验收。

## 公开资料隔离回归（2026-09-05）

- 内部合同、移动专属说明和原始执行记录移出公开树，个人 QA 不再进入公开文件。
- 公共技术资料和测试依赖的 Golden/搜索输入保留，文档链接已核对；公开桌面与企业发布验收分离。
- 本次 `npm run check:source` 通过：726 个测试通过，2 个条件跳过，类型和 lint 检查无错误，全依赖审计未报告漏洞。
- 新公开历史使用项目署名，增加个人目录、剪贴板引用与提交邮箱的防回归规则；旧私有历史不作为新仓库父提交。
- `check:release:v2` 本地一致性通过；正式 `release:gate:v2` 仍因缺少真实桌面验收与明确批准而按预期阻断。此次资料迁移没有把未完成事项改为通过。
- 此轮没有改变桌面业务实现，并已用脱敏后的源码重新构建 Windows 未签名包：ASAR、fuses、随包许可和签名状态检查通过；自动化、5 个 Skill、记忆读写与重启保留的真实 UI/服务冒烟通过，使用独立临时 profile。

## Windows 安装程序发布候选复核

- 修复 Forge 适配器的 `make` 入口：Forge 的安装程序模块被 `require` 加载时不会自动执行，现在显式解析参数、调用并等待生成过程，异常会返回失败状态。相关回归测试共 6 项通过。
- 本地 `npm run check:source` 通过：730 项测试通过，2 项条件跳过，类型检查通过，依赖审计未报告漏洞；保留既有 CSS 警告。
- 使用默认 OpenERX 配置重新生成 Windows x64 的 `OpenERXSetup.exe`（包内版本 2.0.1），没有使用商店 MSIX 或品牌覆盖配置。
- `npm run check:package` 通过：ASAR、Electron fuses、随包声明、自动更新关闭状态，以及真实包的自动化列表、5 个 Skill、记忆保存与重启读取均已检查。使用独立临时 profile，未修改日常数据。
- 对 ASAR 内 64 个文本文件执行公开内容规则扫描，未检出规则覆盖的高置信凭据或个人目录信息；这不替代人工许可和隐私审核。
- 安装程序与应用未签名。本轮没有执行安装程序的干净安装、升级、卸载或回滚验收；包内应用启动通过不等于这些安装流程已通过。
- 本地验证不替代 GitHub CI。正式发布仍需实际许可批准记录及适用的发布验收，不将商店认证自动视为独立 EXE 的签名或开源许可批准。
