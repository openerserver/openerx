{
  "name": "@openerx/web-ui-bff",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test:integration:completion-sync": "bun test tests/opencode-completion-sync.test.ts --timeout 120000"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.0",
    "@opencode-ai/sdk": "^1.0.0",
    "hono": "^4.7.0",
    "jose": "^6.0.0",
    "yaml": "^2.8.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
